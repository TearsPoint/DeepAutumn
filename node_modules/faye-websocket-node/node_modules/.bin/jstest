#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jstest/bin/jstest" "$@"
  ret=$?
else 
  node  "$basedir/../jstest/bin/jstest" "$@"
  ret=$?
fi
exit $ret
